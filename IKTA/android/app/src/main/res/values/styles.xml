<resources>
   <!-- Splash screen theme -->
    <style name="BootTheme" parent="AppTheme">  <!-- SplashTheme-->
        <item name="android:background">@drawable/bootsplash</item> <!--android:windowBackground and name@drawable-->
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
    </style>
</resources>
